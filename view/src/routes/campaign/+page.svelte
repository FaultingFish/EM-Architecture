<script lang="ts">
  // Campaign config form.
  // TODO:
  //   - Fetch GET /projects (Develop) and let user pick one + version
  //   - Optional: open Assembly view, let them pick a target instruction
  //   - GridConfig + SweepConfig components
  //   - On submit: POST /campaigns to Control → redirect to /campaign/[id]
</script>

<h2>New Campaign</h2>
<p>TODO: campaign config form.</p>
