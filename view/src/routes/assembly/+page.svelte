<script lang="ts">
  // Standalone assembly viewer. Mirrors Develop's /projects/[id]/asm page so
  // users can pick targets without leaving View.
  // TODO: project + build pickers, target picker UI.
</script>

<h2>Assembly</h2>
<p>TODO: pick a project + build, render AssemblyListing, click to set target.</p>
